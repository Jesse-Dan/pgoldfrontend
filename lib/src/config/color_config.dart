import 'package:flutter/material.dart';

class ColorConfig {
  // --- PRIMARY & ACCENT COLORS ---

  // The main dark blue color used for the header/balance background and buttons.
  static const Color primaryBlueDark = Color(0xFF1D328C);

  // The lighter blue/indigo shade, possibly for gradients or secondary UI elements.
  static const Color primaryBlueLight = Color(0xFF0052FF);

  // The vibrant yellow/gold color used for the referral section and possibly highlights (P-Pesa logo).
  static const Color accentYellow = Color(0xFFFCBF00);

  // --- BACKGROUND & SURFACE COLORS ---

  // The main background color of the app (the light white/off-white color).
  static const Color surfaceWhite = Color(0xFFFFFFFF);

  // The light grey/off-white used for the main screen background and quick action icons background.
  static const Color backgroundLightGrey = Color(0xFFF5F5F5);

  // --- TEXT & ICON COLORS ---

  // White used for text and icons on the dark blue background.
  static const Color textWhite = Color(0xFFFFFFFF);

  // Dark/black used for most text, such as "Home", "Hello Bafreka", and "Quick Action".
  static const Color textDark = Color(0xFF000000);

  // Secondary gray text color, used for "Top of the morning to you" and navigation labels.
  static const Color textGrey = Color(0xFF707070);

  // --- ACTION & SERVICE COLORS (Quick Actions) ---

  // Crypto Icon background (a light green/mint)
  static const Color actionCrypto = Color(0xFFE5F7E0);

  // Giftcards Icon background (a light lavender/purple)
  static const Color actionGiftcards = Color(0xFFF0E5F7);

  // Top Up Icon background (a light orange/peach)
  static const Color actionTopUp = Color(0xFFFAE5E0);

  // Cable TV Icon background (a light cyan/blue)
  static const Color actionCableTV = Color(0xFFE0F7F7);

  // Electricity Icon background (a light pink/magenta)
  static const Color actionElectricity = Color(0xFFF7E0E5);

  // Betting Icon background (a light pastel blue)
  static const Color actionBetting = Color(0xFFE0EAF7);

  // Flight Icon background (a light pastel green/cyan)
  static const Color actionFlight = Color(0xFFE0F7E5);

  // User Rank Icon background (a light pastel red/star)
  static const Color actionUserRank = Color(0xFFF7E0E0);

  // General Scaffold Background color
  static const Color scaffoldBackground = Color(0xFFF7F9FC);
}
